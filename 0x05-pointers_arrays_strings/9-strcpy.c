#include "main.h"
#include <string.h>

/**
 * _strcpy - copies string
 *
 * @dest: destination
 * @src: string to be copied
 *
 * Return: dest
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
