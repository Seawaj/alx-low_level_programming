#include "main.h"
#include <ctype.h>

/**
 * _islower - the function
 *
 * @c: 'Takes any integer (c)'
 *
 * Return: 1 Always (Success)
 */
int int _isalpha(int c)
{
	if (isalpha(c) == 0)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
