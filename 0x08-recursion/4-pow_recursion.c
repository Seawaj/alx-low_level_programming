#include "main.h"
#include <math.h>

/**
 * _pow_recursion - finds the power of any number
 *
 * @x: any number
 * @y: any number
 *
 * Return: integer
 */
int _pow_recursion(int x, int y)
{
	return (pow(x, y));
}
