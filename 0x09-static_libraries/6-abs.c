#include "main.h"
#include <stdlib.h>

/**
 * _abs - function to return absolute value of an integer
 *
 * @a: any integer
 *
 * Return: absolute value of @a
 */
int _abs(int a)
{
	int i;

	i = abs(a);
	return (i);
}
